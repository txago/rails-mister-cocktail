<h3 class="my-4 pt-4">Add a new dose</h3>
<%= simple_form_for [cocktail, dose] do |f| %>
  <%= f.association :ingredient, class: 'form-control' %>
  <%= f.input :description, class: 'form-control' %>
  <%= f.submit :submit, class: 'btn btn-primary mt-2 mb-4' %>
<% end %>
